---
"kobun": patch
---

The collection editor now answers an unconnected repository — or a missing or invalid Kobun config — the same way every other content page does, by sending you to setup, instead of a bare 404 or 422.
