---
"kobun": patch
---

The collection editor now answers an unconnected repository, or a missing or invalid kobun config, the same way every other content page does — setup for the first, the dashboard that reports the problem for the second — instead of a bare 404 or 422.
