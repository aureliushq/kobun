import { BASE_PATH } from '@rescribe/common'

// const BASE_PATH = '/rescribe'
export const PATHS = {
	BASE: BASE_PATH,
	COLLECTIONS: 'collections',
	EDITOR: 'editor/collections',
	SETTINGS: 'settings',
}
