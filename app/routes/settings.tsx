/**
 * The account settings page. Its sections — profile, Preferences, email,
 * sessions, delete account — are #135; this is the frame's tenant until then.
 */
const Settings = () => {
	return (
		<>
			<h1 className="font-semibold text-2xl">Settings</h1>
			<p className="text-muted-foreground text-sm">
				Your account settings live here. There is nothing to change yet.
			</p>
		</>
	)
}

export default Settings
